name: Build and Push to GHCR

on:
  push:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Set version
      run: echo "VERSION=$(date +%Y%m%d%H%M)" >> $GITHUB_ENV

    - name: Login to GHCR
      uses: docker/login-action@v3
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Build and Push
      uses: docker/build-push-action@v5
      with:
        context: ./web
        push: true
        tags: |
          ghcr.io/${{ github.repository_owner }}/invisia:web-latest
          ghcr.io/${{ github.repository_owner }}/invisia:web-${{ env.VERSION }}
